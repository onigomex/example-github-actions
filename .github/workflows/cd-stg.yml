name: CD-stg

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - master

jobs:
  deploy-stg:
    uses: onigomex/example-github-actions/.github/workflows/cd.yml@reusing-workflows
    with:
      env: stg

